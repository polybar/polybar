add_sources(files
    bspwm.cpp
    command.cpp
    concurrency.cpp
    env.cpp
    factory.cpp
    file.cpp
    inotify.cpp
    io.cpp
    plugin.cpp
    process.cpp
    restack.cpp
    socket.cpp
    string.cpp
    throttle.cpp)

if(ENABLE_I3)
  make_library(polybar-utils-i3
    SHARED
    SOURCES
      i3.cpp
    INCLUDE_DIRS
      ${dirs}
      ${I3IPCPP_INCLUDE_DIRS}
    RAW_DEPENDS
      ${libs}
      ${I3IPCPP_LIBRARIES})
endif()
